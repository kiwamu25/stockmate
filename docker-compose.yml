services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_DSN: "sqlite:./data/app.db"
      PORT: 8080
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
